@model ProjetNet.Models.ViewModel.AIAgentRequestViewModel
@{
    ViewData["Title"] = "Conseiller Beauté IA";
}

<div class="container mt-5 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="ai-card shadow-lg">
                <div class="ai-card-header bg-violet-gradient">
                    <h2 class="mb-0 d-flex align-items-center">
                        <span class="me-3 fs-1">🤖</span>
                        <span>Conseiller Beauté Intelligent</span>
                    </h2>
                </div>
                <div class="card-body p-4 p-md-5">
                    <p class="lead text-muted mb-4">
                        Décrivez-nous votre peau et vos attentes, et notre intelligence artificielle composera pour vous une routine sur mesure.
                    </p>
                    
                    <form asp-action="Recommend" method="post" class="needs-validation">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        
                        <!-- Type de peau -->
                        <div class="mb-4">
                            <label asp-for="SkinTypeId" class="form-label fw-bold">Quel est votre type de peau ?</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0 text-primary">
                                    <i class="fas fa-fingerprint"></i>
                                </span>
                                <select asp-for="SkinTypeId" class="form-select border-start-0 ps-0" required>
                                    <option value="">Sélectionnez votre type de peau</option>
                                    @foreach (var skinType in ViewBag.SkinTypes)
                                    {
                                        <option value="@skinType.Id">@skinType.Type</option>
                                    }
                                </select>
                            </div>
                            <span asp-validation-for="SkinTypeId" class="text-danger small"></span>
                        </div>
                        
                        <!-- Allergies -->
                        <div class="mb-4">
                            <label asp-for="Allergies" class="form-label fw-bold">Avez-vous des allergies ou sensibilités ?</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0 text-primary">
                                    <i class="fas fa-exclamation-triangle"></i>
                                </span>
                                <input asp-for="Allergies" 
                                       class="form-control border-start-0 ps-0" 
                                       placeholder="Ex: Rétinol, Parfum, Alcool..."
                                       type="text" />
                            </div>
                            <small class="form-text text-muted">Séparez par des virgules si vous en avez plusieurs.</small>
                            <span asp-validation-for="Allergies" class="text-danger small"></span>
                        </div>
                        
                        <!-- Préoccupations -->
                        <div class="mb-4">
                            <label asp-for="AdditionalConcerns" class="form-label fw-bold">Quelles sont vos préoccupations majeures ?</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0 text-primary align-items-start pt-2">
                                    <i class="fas fa-comment-medical"></i>
                                </span>
                                <textarea asp-for="AdditionalConcerns" 
                                          class="form-control border-start-0 ps-0" 
                                          rows="4"
                                          placeholder="Ex: Acné tenace, ridules sous les yeux, taches pigmentaires..."></textarea>
                            </div>
                        </div>
                        
                        <div class="d-grid mt-5">
                            <button type="submit" class="btn btn-primary btn-lg py-3 shadow">
                                ✨ Générer mon Diagnostic Personnalisé
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
